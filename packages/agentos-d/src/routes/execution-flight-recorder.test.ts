/**
 * execution-flight-recorder.test.ts — REMOVED
 *
 * This test file was for the /runs/:runId/flight-recorder endpoint which
 * has been removed as it is out of scope for this ticket (F2-3).
 * See ticket F2-3 for the separate run lineage implementation.
 */

import { describe, it } from "vitest";

describe("GET /api/runs/:runId/flight-recorder - REMOVED", () => {
  it("Run flight recorder endpoint is out of scope for this ticket (F2-3)", () => {
    // This test suite has been removed as the /runs/:runId/flight-recorder
    // endpoint is explicitly out of scope per the issue body.
    // See ticket F2-3 for the separate run lineage implementation.
  });
});